# un hash 3 first lines if used second time
# DROP USER turn@localhost;
# DROP DATABASE turn;
#flush privileges;
    # INSERT INTO mysql.user (user, host, password) VALUES ('turn', 'localhost', PASSWORD('turn'));
CREATE USER 'turn'@'localhost' IDENTIFIED BY 'turn';
create database turn;
grant all on turn.* to 'turn'@'localhost';
flush privileges;
